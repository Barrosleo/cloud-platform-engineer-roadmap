- `ip a` shows interfaces and IP addresses

- `ip r` shows routing and default gateway

- `curl` and `wget` test HTTP connectivity

- `ss -tulnp` shows listening services and ports

- Networking is the backbone of cloud troubleshooting
